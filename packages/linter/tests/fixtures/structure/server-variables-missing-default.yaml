openapi: 3.0.3
info:
  title: T
  version: "1.0.0"
servers:
  - url: https://{host}.example.com
    variables:
      host:
        description: no default here
paths:
  /pets:
    get:
      operationId: listPets
      tags: [a]
      description: x
      responses:
        '200':
          description: OK
