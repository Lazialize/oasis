openapi: 3.1.0
info:
  title: T
  version: "1.0.0"
paths:
  /pets:
    get:
      operationId: listPets
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
components:
  schemas:
    BooleanExclusive:
      type: number
      minimum: 0
      exclusiveMinimum: true

    MapExclusive31:
      type: number
      minimum: 0
      exclusiveMinimum: {}

    SeqExclusive31:
      type: number
      maximum: 10
      exclusiveMaximum: []

    NullExclusive31:
      type: number
      minimum: 0
      exclusiveMinimum: null

    StringExclusive31:
      type: number
      maximum: 10
      exclusiveMaximum: "5"

    InvalidTypeInArray:
      type: [string, potato]

    DuplicateTypeArray:
      type: [string, string]

    TypeNotStringOrArray:
      type: 42

    ItemsArrayIn31:
      type: array
      items:
        - type: string

    MinItemsGreaterThanMax:
      type: array
      minItems: 5
      maxItems: 2

    PropertiesNotObject:
      type: object
      properties: nope

    FormatNotString:
      type: string
      format: 123

    WithRefSiblingOk:
      type: object
      properties:
        child:
          $ref: '#/components/schemas/DuplicateTypeArray'
          description: siblings alongside $ref are legal in 3.1

    EmptyTypeArray:
      type: []
