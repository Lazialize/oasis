openapi: 3.0.3
info:
  title: T
  version: "1.0.0"
security:
  - keyAuth: [read:pets]
paths:
  /pets:
    get:
      operationId: listPets
      tags: [a]
      description: x
      responses:
        '200':
          description: OK
components:
  securitySchemes:
    keyAuth:
      type: apiKey
      name: X-Key
      in: header
