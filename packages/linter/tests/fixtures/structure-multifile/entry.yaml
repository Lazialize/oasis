openapi: 3.0.3
info:
  title: T
  version: "1.0.0"
paths:
  /pets:
    get:
      operationId: listPets
      tags: [a]
      description: x
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  name:
                    type: string
              examples:
                pet:
                  $ref: './examples.yaml#/BadExample'
components:
  securitySchemes:
    ApiKeyAuth:
      $ref: './security-schemes.yaml#/BadApiKey'
