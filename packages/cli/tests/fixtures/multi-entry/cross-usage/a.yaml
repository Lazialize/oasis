openapi: 3.0.3
info:
  title: Service A
  version: "1.0.0"
paths:
  /foo:
    get:
      operationId: getFoo
      tags: [foo]
      description: Get a foo.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: "./components.yaml#/components/schemas/Foo"
