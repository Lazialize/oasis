openapi: 3.0.3
info:
  title: CLI Bundle Dereference Cycle Fixture
  version: "1.0.0"
paths:
  /nodes:
    get:
      operationId: listNodes
      tags: [nodes]
      description: List nodes.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Node'
components:
  schemas:
    Node:
      type: object
      properties:
        value:
          type: string
        next:
          $ref: '#/components/schemas/Node'
