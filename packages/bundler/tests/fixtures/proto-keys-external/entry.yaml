openapi: 3.1.0
info:
  title: Proto Keys External
  version: "1.0.0"
paths:
  /widget:
    get:
      operationId: getWidget
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: './shared.yaml#/components/schemas/__proto__'
