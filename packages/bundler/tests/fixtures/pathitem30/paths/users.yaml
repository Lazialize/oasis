get:
  operationId: listUsers
  tags: [users]
  description: List users.
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../shared.yaml#/components/schemas/User'
