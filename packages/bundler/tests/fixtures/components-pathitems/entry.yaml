openapi: 3.1.0
info:
  title: Components PathItems
  version: "1.0.0"
paths:
  /ping:
    get:
      operationId: ping
      tags: [ping]
      description: Ping.
      responses:
        '200':
          description: OK
components:
  pathItems:
    item:
      get:
        operationId: existingItem
        tags: [existing]
        description: Existing item.
        responses:
          '200':
            description: OK
    Common:
      $ref: './item.yaml'
    Fragment:
      $ref: './more.yaml#/components/pathItems/Special'
