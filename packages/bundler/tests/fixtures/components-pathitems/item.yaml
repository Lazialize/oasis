get:
  operationId: getItem
  tags: [item]
  description: Get item.
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: './schema.yaml#/components/schemas/Thing'
