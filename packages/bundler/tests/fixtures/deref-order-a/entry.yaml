openapi: 3.1.0
info:
  title: Deref Order (A before B)
  version: "1.0.0"
paths:
  /ping:
    get:
      operationId: ping
      tags: [ping]
      description: Ping.
      responses:
        '200':
          description: OK
components:
  schemas:
    A:
      type: object
      properties:
        b:
          $ref: '#/components/schemas/B'
    B:
      type: object
      properties:
        value:
          type: string
