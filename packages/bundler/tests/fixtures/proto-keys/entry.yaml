openapi: 3.1.0
info:
  title: Proto Keys
  version: "1.0.0"
  x-meta:
    __proto__: injected
    ok: true
paths:
  /widget:
    get:
      operationId: getWidget
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Holder'
              examples:
                sample:
                  value:
                    __proto__: literal-example
                    ok: true
components:
  schemas:
    __proto__:
      type: string
    Holder:
      type: object
      properties:
        __proto__:
          type: integer
        ok:
          type: boolean
